capture:
  build:
analyze:
  checkers:
    checker-config:
      HARDCODED_CREDENTIALS:
        enabled: false
       # options:
       #  report_empty_credentials: "false"
    concurrency: true
    default: true
    rule: true
  constraint-fpp: true
  parse-warnings:
    enabled: true